import { CloseAppMenuDirective } from './close-app-menu.directive';

describe('CloseAppMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new CloseAppMenuDirective();
    expect(directive).toBeTruthy();
  });
});
