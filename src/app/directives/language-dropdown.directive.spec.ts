import { LanguageDropdownDirective } from './language-dropdown.directive';

describe('LanguageDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new LanguageDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
