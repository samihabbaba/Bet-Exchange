import { CloseBetSlipDirective } from './close-bet-slip.directive';

describe('CloseBetSlipDirective', () => {
  it('should create an instance', () => {
    const directive = new CloseBetSlipDirective();
    expect(directive).toBeTruthy();
  });
});
